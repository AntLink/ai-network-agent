VITE_API_BASE_URL=http://localhost:8000

# Set true untuk memaksa semua hook memakai MSW mock.
# Default false: read-only core akan mencoba FastAPI lalu fallback ke MSW saat dev jika backend gagal.
VITE_USE_MOCKS=false
