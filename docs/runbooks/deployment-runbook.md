# AI Network Agent Production Deployment Runbook

> Single operator-facing source of truth. `RELEASE:` values must be filled from
> a verified release build before production approval. This file documents
> current operational state (Milestones 1-3 verified + M4 in progress); it is
> **not** yet a production approval. Never place secrets in this file.

## 1. Release Identity

- Git commit: `<record release commit>`
- Release manifest template: `docs/production/release-manifest.template.json`
- Release evidence inventory: `docs/evidence/release-readiness/image-digests-20260906.json`
- SBOM/license notice bundle: `<required before production approval>`
- Central release: `development` (FastAPI, uvicorn `app.main:app`)
- Edge release: native Alpine client mode (Go `ainet-edge`, outbound polling)
- Edge protocol version: `1` (`min==max==driver_capability_version==1`)
- Driver capability version: `1`
- DB schema version: `001` (`backend/migrations/001_task_attempts.sql`)
- Overlay/provider version: `not deployed` (replaceable `OverlayProvider` seam only)
- Production gate evidence: `docs/production/production-gate.json` (gates unmet)

## 2. Prerequisites / Network Requirements

- Edge host able to reach the Central mTLS control API (outbound HTTPS, e.g.
  TCP 8443); Central does not SSH directly into customer LAN devices.
- Edge host able to reach managed devices over their data plane (no global route
  of customer LANs into Central). Scoped by customer/site/Edge/device identity
  via `ExecutionRoutingResolver` (M3 verified).
- Redis reachable on `REDIS_URL` for Edge session registry (`EDGE_SESSION_BACKEND=redis`).
- PostgreSQL reachable via `POSTGRES_DSN` when `TASK_ATTEMPT_BACKEND=postgres`;
  apply `backend/migrations/001_task_attempts.sql`.
- Ports: Central API (e.g. 8000/8010), Edge control (9443), Redis (6379), PostgreSQL (5432).

## 3. Central / Database / Redis Deployment

Central (development):
```text
python -m uvicorn app.main:app --app-dir backend --host 0.0.0.0 --port 8000
```
Verify:
```text
curl http://127.0.0.1:8000/api/v1/tasks/capability   # OpenAPI route present
```
Native Edge control routing: configure the Alpine service with Central's mTLS
URL and the Edge client identity:
`--control-url https://<central-host>:8443/api`, `--edge-id`, `--ca-file`,
`--cert-file`, `--key-file`, `--keystore-file`, and `--journal-file`.
The Edge polls `/v1/control/tasks/next`; task execution stays on the Edge LAN.

Redis: `EDGE_SESSION_BACKEND=redis`, `REDIS_URL`; memory backend is test-only.
PostgreSQL TaskAttempt: `TASK_ATTEMPT_BACKEND=postgres` + `POSTGRES_DSN`; default
memory backend is test-only. Live migration/recovery evidence still required.

## 4. PostgreSQL Live Gate Procedure

Do not enable PostgreSQL for ordinary unit tests. For an approved staging
database only:

```text
set RUN_LIVE_POSTGRES=1
set POSTGRES_DSN=postgresql://<user>:<password>@127.0.0.1:5432/<database>
set PYTHONPATH=backend
.\env\Scripts\python.exe -c "from pathlib import Path; print(Path('backend/migrations/001_task_attempts.sql').read_text())"
.\env\Scripts\python.exe -m pytest backend/tests/test_postgres_live.py -q
```

The migration must be applied by the approved database operator, followed by
evidence for concurrent single-owner claim, renewal, expiry recovery, and a
backup/restore rehearsal. Never commit or log the DSN/password.

## 5. Overlay / Controller Deployment

- Optional for the current control path. Central ↔ Edge uses direct HTTPS/mTLS;
  ZeroTier is not required for normal task dispatch. If enabled, install the
  ZeroTier client on the Edge and manage membership through the private
  controller via `OverlayProvider` in `backend/app/services/overlay.py`.
  Controller/auth tokens remain private; never expose raw controller APIs to
  browser/Internet. Defer private-root work and licensing review until the
  launch milestone.

## 6. PKI Preparation

- Development-only PKI is generated by `tools/dev_pki.py` and
  `tools/gen_m1_live_pki.py` (`tmp/<pkidir>/`): `ca.crt/key`, `central.crt/key`,
  `edge-001.crt/key`.
- Role correct: `central` and `edge-001` certs carry SERVER_AUTH + CLIENT_AUTH;
  edge cert SAN includes the Edge reachable IP.
- Distinguish mTLS identity (project PKI) from overlay identity (invariant #20).
- Production requires durable CA, rotation/revocation, and enrollment evidence
  (not met).
- The Central application revocation registry is
  `EDGE_CERT_REVOCATION_STATE_FILE`. A production mTLS terminator must verify
  the client certificate, calculate its SHA-256 fingerprint, check durable
  revocation state before proxying HTTP, strip any incoming
  `X-Client-Cert-Fingerprint`, and set that header from the verified peer
  certificate. Bind the FastAPI listener to the private proxy network; never
  expose the application identity header directly to untrusted clients.
- Central rejects a revoked fingerprint at HELLO and the revoke workflow
  disconnects active sessions. A live TLS-terminator rejection test remains
  required before production approval.
- Reference terminator configuration: `deploy/mtls/nginx.conf.example`.
  The `ssl_crl` file must be regenerated and the terminator reloaded after a
  revoke. The application listener must remain private behind this terminator.
- A dynamic pre-HTTP reference gateway is implemented at
  `deploy/mtls/revocation_gateway.py`; it reloads the durable registry per
  connection and closes revoked peers before proxying. It remains a reference
  runtime until supervised deployment, HA behavior, resource limits, and
  monitoring are proven.
- The disposable container validation proved live registry reload without a
  gateway restart. Before production use, add a supervisor health check,
  bounded request/connection limits, structured denial metrics, and a tested
  multi-node/shared-state deployment.
- The existing compose `mtls` profile now applies restart policy, non-root
  read-only runtime, read-only PKI/revocation mounts, a TCP liveness check,
  no-new-privileges, and container resource limits. The TCP check is only
  liveness; production readiness still requires an authenticated readiness
  probe and denial metrics.
- Container packaging is available in `deploy/mtls/Dockerfile` and the
  existing `backend/docker-compose.yml` under the disabled `mtls` profile.
  Start it only after mounting production PKI at `runtime/mtls` and verifying
  the revocation-state volume; do not expose the API service directly on the
  public interface.

## 7. Edge Artifact Verification / Installation / Enrollment

- Build (Go):
```text
cd edge && go test ./...
```
- Deploy to Edge host (GNS3 VM example): copy `tmp/ainet-edge-linux` + PKI to
  `/opt/ainet-edge/`, run as a systemd service:
```text
/opt/ainet-edge/ainet-edge --control-listen 0.0.0.0:9443 --edge-id edge-001 \
  --ca-file /opt/ainet-edge/ca.crt --cert-file /opt/ainet-edge/edge.crt \
  --key-file /opt/ainet-edge/edge.key --keystore-file /opt/ainet-edge/keystore.json \
  --journal-file /opt/ainet-edge/journal.json
```
- For client-mode Edge connecting through the production Nginx mTLS endpoint,
  set the public TLS name explicitly (the default `central` is for the lab):
```text
ainet-edge.exe --control-url https://edge-control.antlinx.com \
  --control-server-name edge-control.antlinx.com --edge-id edge-001 \
  --boot-id <unique-boot-id> --ca-file <edge-client-ca.crt> \
  --cert-file <edge-001.crt> --key-file <edge-001.key> \
  --keystore-file <keystore.json> --journal-file <journal.json>
```
- Verify listener: `systemctl is-active ainet-edge` and `ss -ltnp | grep 9443`.
- Enrollment (mTLS): Central outbound client performs HELLO/READY over TLS 1.3;
  evidence: `docs/evidence/m1-vertical-slice/`.

## 7. Credential Provisioning / Local Keystore Verification

- Provision `credential_ref` entries into the Edge-local keystore
  (`/etc/ainet-edge/keystore.json`), each with `username`, `password`,
  `host_key` (pinned device SSH host key). Never store secrets in task JSON,
  logs, or this runbook.
- Do not use TOFU / `StrictHostKeyChecking=no` / shared known_hosts.
- Fail closed on missing/stale/unauthorized `credential_ref`; no arbitrary
  default credential fallback.

## 8. Device Onboarding / Execution-Routing Verification

- Register device with `execution_location`, `customer_id`, `site_id`,
  `edge_id` (e.g. `r1-m1` with `edge_id=edge-001`).
- Submit a capability:
```text
POST /api/v1/tasks/capability
{"device_id": "r1-m1", "capability": "device.read.facts",
 "credential_ref": "r1-lab", "execution_location": "EDGE",
 "edge_id": "edge-001", "idempotency_key": "<key>"}
```
- Verify route = `EDGE, edge-001`, task status success, normalized result,
  audit event. Evidence: `docs/evidence/m1-vertical-slice/`.

## 9. Overlapping-Subnet Acceptance

- Two devices sharing a management IP behind different Edges must scope by
  owning Edge (M3): e.g. `a-r1`(192.168.1.1, edge-001) and `b-r1`(192.168.1.1,
  edge-b). Requesting the wrong Edge or wrong customer/site is rejected (422):
```text
POST /api/v1/tasks/capability {device_id a-r1, edge_id edge-b} -> 422
```
- Evidence: `docs/evidence/m3-multisite-overlapping-subnet/`.
- Live positive two-Edge dispatch is verified in
  `docs/evidence/gns3-overlap/live-two-edge-proof-20260905.json` for R1/R2,
  both using `192.168.1.1` behind separate native Alpine Edges.

## 10. Safety / Approval / Backup / Apply / Verify / Rollback Acceptance

- Normal automation is capability-based; Edge envelopes reject secret payload
  fields.
- Enforcement gate: `backend/app/services/task_execution_gate.py` (fail-closed).
- Approval tokens: `backend/app/services/approval_verifier.py`; retry/replay
  decisions require authenticated operator + `operator_approval_ref` + HMAC token.
- Test evidence: `backend/tests/test_retry_api.py`, `test_edge_control.py`,
  `test_approval_verifier.py`, `test_task_execution_gate.py`, `test_replay_gate.py`.
- Device configuration rollback exists at `/api/v1/config/rollback` (driver
  transaction). Edge binary rollback is Milestone 4 pending.

## 11. Monitoring / SLO / Alert Verification

- Rule-based alert evaluation (M4 slice): `POST /api/v1/alerts/evaluate` with
  `{"device_id", "metrics": {"cpu", "memory"}}` returns triggered alerts by
  severity. Evidence: `docs/evidence/m4-production-operations/`.
- Alert rules: default CPU `>75` warn / `>90` critical; memory `>80` warn /
  `>90` critical.
- Monitoring collection is currently on-demand (`/api/v1/monitoring/{device_id}`);
  a production collector/poller + persistence + notification is pending.

## 12. Backup and Restore Drill

- Configuration backup exists: `POST /api/v1/backups` saves `.cfg` files;
  LIST/DOWNLOAD/DELETE are available.
- Device restore endpoint from backup is **not implemented**.
- PostgreSQL backup/restore drill is verified at
  `docs/evidence/m5-scale-ha/postgres-restore-drill-20260905.json`.
  Controller/PKI/application recovery drill remains required before production
  approval.

## 13. HA / Failover Verification

- Edge session registry selectable via Redis (`EDGE_SESSION_BACKEND=redis`);
  memory is single-node/M1 test-only.
- Distributed control routing and Central-node-loss failover evidence pending
  (Milestone 5).

## 14. Production Gate

```text
python .opencode/skills/ainet-zerotier-platform/scripts/production_gate.py --config docs/production/production-gate.json
```
Result/evidence: **FAIL / NOT READY** until the missing gates below pass
(see release evidence `docs/evidence/production-gates/`).

## 15. Rollback Procedure

- Device config: use `/api/v1/config/rollback` (driver transaction) after a
  pre-change backup.
- Edge software: keep last-known-good binary; on service/start/health failure,
  restore previous binary and re-run enrollment. Milestone 4 pending.

## 16. Quarantine / Revoke / Offboarding

- Soft quarantine: `POST /api/v1/control/quarantine` (recorded, blocks
  privileged Edge execution; recovery channel preserved).
- Clear quarantine: `POST /api/v1/control/clear-quarantine` (`network-admin`).
- Hard revoke: `POST /api/v1/control/revoke` disconnects sessions + blocks
  future HELLO. Production revoke must also bind to durable PKI certificate
  invalidation (PKI-REVOCATION-005 pending).
- Nginx CRL deployment: after the revocation authority publishes a new PEM
  CRL, run `python deploy/mtls/nginx_crl_reload.py --crl /etc/ainet/pki/ca.crl
  --hash-state /var/lib/ainet/nginx-crl.sha256`. The helper validates that the
  CRL is parseable and unexpired, runs `nginx -t`, then reloads Nginx only when
  the CRL hash changed. Invalid/expired CRLs fail closed without reload.
- Persistent staging terminator is available through the disabled Compose
  profile `nginx-staging` on port `9444`. It requires secret-mounted
  `runtime/mtls/ca.crl` before startup; start with
  `docker compose -f backend/docker-compose.yml --profile nginx-staging up -d
  --build nginx-staging`. Do not expose this profile as production until PKI
  ownership, reload supervision, monitoring, and rollback are approved.
- Offboarding: distinct `DELETED` state; revoked/deleted identities are not
  re-activatable.

## 17. Incident / Operational Ownership

- Record: documented owner/on-call process required before production; no
  secrets. Incident flow: reconcile unknown attempts before any retry; never
  auto-replay uncertain execution.

## 18. Licensing / Component Matrix

- Maintain per-component licensing/commercial rights separately from the
  deployment topology claim. Self-hosted does not imply all-open/free;
  record upstream/license evidence in `docs/licensing-matrix.md`.
- Before release approval, complete the release manifest, pin immutable image
  digests, attach SBOM and notices, and record the approved licensing
  decision/reference in `docs/production/production-gate.json`.
- Apply `docs/production/image-signing-policy.md`; local Docker tags are not
  deployable release identities.
- Follow `docs/runbooks/release-signing-runbook.md` once the approved registry
  and signing identity are available.

## 19. Production Host Deployment Plan

> Inspected Ubuntu host 192.168.210.51 (Ubuntu 24.04.4 LTS, Docker active).
> Findings recorded in session log `2026-09-03_1957_phase-04_five-block-production-hardening.md`
> (commit `8be8ccf`). Existing Nginx owns TCP 443 and must be preserved. There
> is no existing AINET deployment to preserve.

### 19.1 Blocking gates before any deployment (all fail-closed)

1. **Registry feed auth** – GHCR images are private; anonymous pull returns
   `unauthorized`. Provision a read-only PAT (`read:packages`) as a host
   secret (`docker login ghcr.io --username <user> --password-stdin`) by the
   operator. Never place the PAT in the repository, chat, or session logs.
2. **Production hostname** – e.g. `edge-control.antlinx.com`; must be approved
   and must match the served certificate SAN.
3. **Production TLS certificate** for that hostname (public CA or approved
   private CA chain with renewal/revocation path). Lab PKI is not sufficient.
4. **Listener/port plan** – do not modify the existing Nginx/443 service.
   AINET listeners must use dedicated ports/addresses approved for coexistence
   (Central API, Edge control, Redis, PostgreSQL) without clobbering existing
   workloads.
5. **PKI revocation policy** – approve `docs/production/revocation-policy.md`
   and bind production revocation state to the durable registry + CRL:
   `EDGE_CERT_REVOCATION_STATE_FILE`, terminator `ssl_crl` via
   `deploy/mtls/nginx_crl_reload.py`.
6. **Licensing/commercial approval** – record the approved decision reference
   (pattern `LIC-YYYY-MM-DD-NNN-<LABEL>`) before deployment.

### 19.2 Host preflight

Run the read-only preflight on the target host; it never writes, pulls, or
modifies anything and fails closed on every blocked gate:

```text
python3 deploy/production/preflight.py \
  --hostname edge-control.antlinx.com \
  --tls-cert /etc/ainet/pki/central.crt \
  --manifest docs/production/release-manifest-v0.1.0.json \
  --revocation-policy docs/production/revocation-policy.md \
  --licensing-ref <approved LIC reference>
```

`VERDICT: READY` (exit 0) is required before proceeding.
`READY-WITH-WARNINGS` (exit 2) still requires manual capacity/listener review;
`NOT-READY` (exit 1) blocks deployment. Contract tests:
`backend/tests/test_production_preflight.py`.

### 19.3 Capacity and isolation review

- Host showed ~3.3 GiB RAM with high swap usage. A full
  Central/PostgreSQL/Redis/controller deployment must be
  capacity- and blast-radius-reviewed before touching existing workloads.
- Prefer reverse proxies/containers pinned to dedicated loopback/private
  listener addresses; keep the existing Nginx/443 tenant untouched.

### 19.4 Post-deployment hygiene

- Change the host access password/keys after deployment completes and revoke
  any temporary provisioning credentials.
- Re-run the preflight after any host-level change and record the verdict
  plus evidence in `docs/evidence/production-gates/`.
